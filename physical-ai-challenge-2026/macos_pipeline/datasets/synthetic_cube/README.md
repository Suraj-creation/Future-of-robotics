Synthetic dataset output

This directory is created by `capture_and_view.py` and holds RGB (PNG)
and metadata (TXT) frames used for training YOLOv8 / DenseFusion.

Run `capture_and_view.py` from the `mujoco_demo` folder to populate this.
